#import <Foundation/Foundation.h>

int main14 (int argc, const char * argv[]) {
    @autoreleasepool {
        int sum;
        // compute result
        sum = 25 + 37 - 19;
        // display results
        NSLog (@"The sum is %i", sum);
    }
    return 0;
}